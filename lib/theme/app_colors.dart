import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - Royal Blue Theme
  static const primary = Color(0xFF1E3A8A); // Royal Blue
  static const onPrimary = Color(0xFFFFFFFF);
  static const primaryContainer = Color(0xFFD1E3FF);
  static const onPrimaryContainer = Color(0xFF001A41);

  // Secondary Colors - Complementary Blue
  static const secondary = Color(0xFF2563EB);
  static const onSecondary = Color(0xFFFFFFFF);
  static const secondaryContainer = Color(0xFFDBE2FF);
  static const onSecondaryContainer = Color(0xFF001A41);

  // Tertiary Colors - Accent Blue
  static const tertiary = Color(0xFF3B82F6);
  static const onTertiary = Color(0xFFFFFFFF);
  static const tertiaryContainer = Color(0xFFE0F2FF);
  static const onTertiaryContainer = Color(0xFF001A41);

  // Error Colors
  static const error = Color(0xFFDC2626);
  static const onError = Color(0xFFFFFFFF);
  static const errorContainer = Color(0xFFFEE2E2);
  static const onErrorContainer = Color(0xFF7F1D1D);

  // Background Colors
  static const background = Color(0xFFF8FAFC);
  static const onBackground = Color(0xFF0F172A);
  static const surface = Color(0xFFFFFFFF);
  static const onSurface = Color(0xFF0F172A);
  static const surfaceVariant = Color(0xFFF1F5F9);
  static const onSurfaceVariant = Color(0xFF334155);

  // Outline Colors
  static const outline = Color(0xFF94A3B8);
  static const outlineVariant = Color(0xFFCBD5E1);

  // Shadow Colors
  static const shadow = Color(0xFF000000);
  static const scrim = Color(0xFF000000);

  // Inverse Colors
  static const inverseSurface = Color(0xFF1E293B);
  static const onInverseSurface = Color(0xFFF8FAFC);
  static const inversePrimary = Color(0xFF60A5FA);

  // Surface Tint
  static const surfaceTint = Color(0xFF1E3A8A);

  // Dark Theme Colors
  static const darkPrimary = Color(0xFF60A5FA);
  static const darkOnPrimary = Color(0xFF1E3A8A);
  static const darkPrimaryContainer = Color(0xFF1E40AF);
  static const darkOnPrimaryContainer = Color(0xFFD1E3FF);

  static const darkSecondary = Color(0xFF93C5FD);
  static const darkOnSecondary = Color(0xFF1E40AF);
  static const darkSecondaryContainer = Color(0xFF1E40AF);
  static const darkOnSecondaryContainer = Color(0xFFDBE2FF);

  static const darkTertiary = Color(0xFF60A5FA);
  static const darkOnTertiary = Color(0xFF1E40AF);
  static const darkTertiaryContainer = Color(0xFF1E40AF);
  static const darkOnTertiaryContainer = Color(0xFFE0F2FF);

  static const darkError = Color(0xFFFCA5A5);
  static const darkOnError = Color(0xFF7F1D1D);
  static const darkErrorContainer = Color(0xFF991B1B);
  static const darkOnErrorContainer = Color(0xFFFEE2E2);

  static const darkBackground = Color(0xFF0F172A);
  static const darkOnBackground = Color(0xFFF8FAFC);
  static const darkSurface = Color(0xFF1E293B);
  static const darkOnSurface = Color(0xFFF8FAFC);
  static const darkSurfaceVariant = Color(0xFF334155);
  static const darkOnSurfaceVariant = Color(0xFFCBD5E1);

  static const darkOutline = Color(0xFF94A3B8);
  static const darkOutlineVariant = Color(0xFF334155);

  static const darkShadow = Color(0xFF000000);
  static const darkScrim = Color(0xFF000000);

  static const darkInverseSurface = Color(0xFFF8FAFC);
  static const darkOnInverseSurface = Color(0xFF1E293B);
  static const darkInversePrimary = Color(0xFF1E3A8A);

  static const darkSurfaceTint = Color(0xFF60A5FA);

  // Success Colors
  static const success = Color(0xFF059669);
  static const onSuccess = Color(0xFFFFFFFF);
  static const successContainer = Color(0xFFD1FAE5);
  static const onSuccessContainer = Color(0xFF065F46);

  // Warning Colors
  static const warning = Color(0xFFD97706);
  static const onWarning = Color(0xFFFFFFFF);
  static const warningContainer = Color(0xFFFEF3C7);
  static const onWarningContainer = Color(0xFF92400E);

  // Info Colors
  static const info = Color(0xFF0284C7);
  static const onInfo = Color(0xFFFFFFFF);
  static const infoContainer = Color(0xFFE0F2FE);
  static const onInfoContainer = Color(0xFF075985);

  // Divider Colors
  static const divider = Color(0xFFE2E8F0);

  // Overlay Colors
  static const overlay = Color(0x52000000);
  static const overlayLight = Color(0x1F000000);
  static const overlayDark = Color(0x99000000);

  // Disabled Colors
  static const disabled = Color(0xFF94A3B8);
  static const onDisabled = Color(0xFF64748B);

  // Focus Colors
  static const focus = Color(0xFF1E3A8A);
  static const focusRing = Color(0x1F1E3A8A);

  // Selected Colors
  static const selected = Color(0xFF1E3A8A);
  static const selectedContainer = Color(0xFFD1E3FF);
  static const onSelected = Color(0xFFFFFFFF);
  static const onSelectedContainer = Color(0xFF001A41);

  // Hover Colors
  static const hover = Color(0xFF1E3A8A);
  static const hoverContainer = Color(0xFFD1E3FF);
  static const onHover = Color(0xFFFFFFFF);
  static const onHoverContainer = Color(0xFF001A41);

  // Pressed Colors
  static const pressed = Color(0xFF1E3A8A);
  static const pressedContainer = Color(0xFFD1E3FF);
  static const onPressed = Color(0xFFFFFFFF);
  static const onPressedContainer = Color(0xFF001A41);

  // Dragged Colors
  static const dragged = Color(0xFF1E3A8A);
  static const draggedContainer = Color(0xFFD1E3FF);
  static const onDragged = Color(0xFFFFFFFF);
  static const onDraggedContainer = Color(0xFF001A41);
} 